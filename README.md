# Spring Boot Webflux Server Sent Events.

Use

```bash
mvn package
```
to build the application.

To get the text stream type
```bash
curl http://localhost:8080/api/time/stream/<INTERVAL IN MILLIS>
```





